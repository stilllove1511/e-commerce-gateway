import { TransformData1Interceptor } from './transform-data1.interceptor';

describe('TransformData1Interceptor', () => {
  it('should be defined', () => {
    expect(new TransformData1Interceptor()).toBeDefined();
  });
});
